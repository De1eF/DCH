class character {
    userId = 0;
    name = "";
    paramsMap = {};
}

function parse_character_params(params) {
    console.log("Parsing character params");
    console.log(params);
    // for (let i = 0; i < params.length; i++) {
    //     local_character.paramsMap[params[i].name] = params[i].value;
    // }
}